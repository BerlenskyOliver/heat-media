import {BarsLoader} from 'components/Icons'

const Loading = () => (
    <div className="w-80 h-80 flex items-center text-center justify-center p-3">
        <BarsLoader />
    </div>
)

export default Loading